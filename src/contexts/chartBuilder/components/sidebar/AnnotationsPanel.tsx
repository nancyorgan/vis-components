/** Barrel: the Annotations panel lives in `./annotations/` (shell + one
 *  module per editor kind + shared controls/sections). Re-exported here so
 *  importers keep the established `sidebar/AnnotationsPanel` path. */
export { AnnotationsPanel } from "./annotations/AnnotationsPanel"
